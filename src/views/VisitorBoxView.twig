{% extends "layouts/master.twig" %}

{% block content %}

    <div class='box'>
        <div class='box_head visitor'>
            <h1 class='title title_1'>{{box.title}}</h1>
            {{user.name}}
            {{user.first_name}}
        </div>  
        <div class='thanks'>
            <h2 class='title title_2 title--bg'>Message de l'auteur du coffret</h2>
            <p>{{box.message}}</p>
        </div>
        
        <h2 class='title title_2 title--bg'>Contenu du coffret</h2>

        <div class='box_grid'>
            {% for offer in offers %}
            <div class='containerBox'>
                <a href='$urlDetailledOffer' class='offer'>
                    <img src='{{ siteUrl('assets/img/offers/' ~ offer.image ) }}'>
                    <div class='offer_bottom'>
                        <h2 class='label label_title'>{{offer.title}}</h2>
                        <div class='offer_bottom_infos'>
                            <p class='label label_category'>{{offer.category}}</p>
                        </div>
                    </div>
                </a>
            </div>
            {% endfor %}
        </div>

        <h2 class='title title_2 title--bg'>Remercier {{user.name}} {{user.first_name}}</h2>
        <form class='form_thank' method='POST'>
            <textarea name='message_return' placeholder='Votre message'></textarea>
            <div class='buttonLayout buttonLayout--right'>
                <button class='button button--green' type='submit'>Remercier</button>
            </div>
        </form>
    </div>

{% endblock %}